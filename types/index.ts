// types/index.ts
/**
 * Central exports for all types
 * Import everything from here: import { User, Product, ... } from "@/types"
 */

// Re-export all enums
export * from './enums';

// Re-export all database types
export * from './db';

// Re-export all API types
export * from './api';

// Re-export all form types
export * from './form';

// Re-export all common types

// Re-export existing types if they exist

// If you have admin types later, add them:
// export * from './admin';